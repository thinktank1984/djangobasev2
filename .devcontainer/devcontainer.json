// For format details, see https://aka.ms/devcontainer.json. 
// For config options, see https://github.com/microsoft/vscode-dev-containers/tree/main/containers/python-3
{
	"name": "python-db-copilot",
	"dockerComposeFile": "docker-compose.yaml",
	"service": "app",
	"workspaceFolder": "/workspace",

	"forwardPorts": [5432],
	"portsAttributes": {
		"5432": { "label": "PostgreSQL port", "onAutoForward": "silent" }
	},

	// 🧩 VS Code settings and extensions
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"charliermarsh.ruff",
				"ms-python.black-formatter",
				"mtxr.sqltools",
				"mtxr.sqltools-driver-pg",
				"ms-vscode.vscode-node-azure-pack",
				"mechatroner.rainbow-csv"
			],
			"settings": {
				"python.defaultInterpreterPath": "/usr/local/bin/python",
				"python.testing.unittestEnabled": false,
				"python.testing.pytestEnabled": false,
				"[python]": {
					"editor.formatOnSave": true,
					"editor.codeActionsOnSave": {
						"source.fixAll": true
					},
					"editor.defaultFormatter": "ms-python.black-formatter"
				},
				"sqltools.connections": [
					{
						"name": "Local database",
						"driver": "PostgreSQL",
						"server": "localhost",
						"port": 5432,
						"database": "postgres",
						"username": "admin",
						"password": "LocalPasswordOnly"
					},
					{
						"name": "Azure database",
						"driver": "PostgreSQL",
						"server": "<HOSTNAME>.postgres.database.azure.com",
						"port": 5432,
						"database": "postgres",
						"username": "<USERNAME>",
						"askForPassword": true,
						"pgOptions": { "ssl": true }
					}
				],
				"files.exclude": {
					".ruff_cache": true,
					".pytest_cache": true
				}
			}
		}
	},

	// ✅ Use uv for dependency installation (fast, cache-aware)
	"postCreateCommand": "if [ -f setup/requirements.txt ]; then uv pip install --system -r setup/requirements.txt; elif [ -f requirements.txt ]; then uv pip install --system -r requirements.txt; fi",
"remoteEnv": {
	"UV_CACHE_DIR": "/tmp/.uv-cache"
},
"remoteUser": "vscode"
}
